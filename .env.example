# Next.js environment variables (can be public if prefixed with NEXT_PUBLIC_)
# For Vercel, these should be set in Project Settings > Environment Variables

# reCAPTCHA v2 Keys (if you implement it fully)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_site_key
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key

# Razorpay Keys
# NEXT_PUBLIC_RAZORPAY_KEY_ID=your_razorpay_key_id (if needed on client for payment UI, not required for this project)
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret

# Email Configuration (Resend or Nodemailer)
# Example for Resend:
# RESEND_API_KEY=your_resend_api_key
# FROM_EMAIL=noreply@yourdomain.com (Email address to send from)

# Employee Email Address
EMPLOYEE_EMAIL_ADDRESS=your_employee_email@example.com
